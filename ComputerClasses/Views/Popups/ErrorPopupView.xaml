<UserControl x:Class="ComputerClasses.Views.Popups.ErrorPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerClasses.ViewModels.Popups"
             xmlns:vm="clr-namespace:ComputerClasses.ViewModels.Popups"
             xmlns:converters="clr-namespace:ComputerClasses.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:ErrorPopupViewModel}">
    <UserControl.Resources>
        <converters:RectConverter x:Key="RectConverter" />
    </UserControl.Resources>
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="0.9*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="0.9*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Grid.Column="1" Background="White" x:Name="Root" >
            <Grid.Clip>
                <RectangleGeometry RadiusX="20" RadiusY="20">
                    <RectangleGeometry.Rect>
                        <MultiBinding Converter="{StaticResource RectConverter}">
                            <Binding ElementName="Root" Path="ActualWidth" />
                            <Binding ElementName="Root" Path="ActualHeight" />
                        </MultiBinding>
                    </RectangleGeometry.Rect>
                </RectangleGeometry>
            </Grid.Clip>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Ошибка" TextAlignment="Center" FontWeight="Bold" FontSize="26" Grid.Row="0" Margin="0 10 0 0" />
            <ScrollViewer Grid.Row="1" VerticalAlignment="Center" >
                <TextBlock FontSize="20" TextWrapping="Wrap" Text="{Binding ErrorMessage}" VerticalAlignment="Center"  TextAlignment="Center" />
            </ScrollViewer>
            <Button 
                    Content="Закрыть" 
                    Command="{Binding CloseCommand}" 
                    Style="{StaticResource RoundedButtonStyle}"
                    VerticalAlignment="Bottom"
                    Margin="0 0 0 20"
                    Grid.Row="2"
                    />
        </Grid>
    </Grid>
</UserControl>
