<UserControl x:Class="ComputerClasses.Views.Notifications.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerClasses.Views.Notifications"
             xmlns:notif="clr-namespace:ComputerClasses.ViewModels.Notifications"
             xmlns:converter="clr-namespace:ComputerClasses.Converters"
             xmlns:anim="clr-namespace:WpfAnimatedGif;assembly=WpfAnimatedGif"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance notif:NotificationsViewModel}">
    <UserControl.Resources>
        <converter:NotificationItemVisabilityConverter x:Key="notifItemVis"/>
    </UserControl.Resources>
    <Grid Height="auto" Width="auto">
        <Button x:Name="not" Width="80" Height="80" Margin="0 0 10 0" VerticalAlignment="Top" HorizontalAlignment="Right" Background="Transparent" Command="{Binding NotificationButton.NavCommand}">
            <WrapPanel>
                <Image Width="80" Height="80" 
                       anim:ImageBehavior.AnimatedSource="{Binding NotificationButton.Image}"
                       anim:ImageBehavior.AutoStart="False">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Loaded">
                            <b:InvokeCommandAction Command="{Binding LoadCommand}" PassEventArgsToCommand="True"/>
                        </b:EventTrigger>
                        <b:EventTrigger EventName="MouseEnter">
                            <b:InvokeCommandAction Command="{Binding PlayCommand}"/>
                        </b:EventTrigger>
                    </b:Interaction.Triggers >
                </Image>
                <Ellipse Visibility="{Binding HasNotifications,Converter={StaticResource notifItemVis}}" Width="10" Height="10" VerticalAlignment="Bottom" Margin="-140 0 0 0" Fill="#00BCD4"/>
            </WrapPanel>
        </Button>
        <ScrollViewer MinHeight="150" MinWidth="120" Margin="0 100 100 0" Visibility="{Binding IsOpen,Converter={StaticResource notifItemVis}}">
            <ItemsControl Background="#B2CACACA" MinHeight="150" MinWidth="120"  ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Visibility="{Binding IsVisible,Converter={StaticResource notifItemVis},Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="auto">
                            <TextBlock Text="{Binding Message}" Width="auto"/>
                            <Button Content="Скрыть" 
                        Command="{Binding DataContext.HideNotificationCommand,
                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding .}"/>
                        </WrapPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
