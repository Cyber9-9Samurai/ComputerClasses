<UserControl x:Class="ComputerClasses.Views.Pages.MainPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerClasses.Views.Pages"
             xmlns:vm="clr-namespace:ComputerClasses.ViewModels.Pages"
             xmlns:converter="clr-namespace:ComputerClasses.Converters"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:MainPageViewModel}">
    <UserControl.Resources>
        <converter:DataGridViewVisabilityConverter x:Key="dataGridVis"/>
        <converter:EmptyDataMessageVisabilityConverter x:Key="emptyDataVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Button Content="Фильтр" Command="{Binding OpenFilterCommand}"/>
            <TextBox MaxWidth="10" Text="{Binding SearchText, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        </WrapPanel>
        <ItemsControl Grid.Column="0" Grid.Row="1" ItemsSource="{Binding OperationButtons}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="200" Height="200" Command="{Binding NavCommand}">
                        <Canvas>
                            <Image Width="100" Height="100" Source="{Binding Image}"/>
                            <TextBlock Text="{Binding Title}"/>
                        </Canvas>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Visibility="{Binding IsExistFile,Converter={StaticResource emptyDataVis}}">
            Файл не нйден , может <Run Foreground="Blue">
                создать
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseDown">
                        <b:InvokeCommandAction Command="{Binding CreateFileCommand}"/>
                    </b:EventTrigger>       
                </b:Interaction.Triggers>
            </Run> новый файл Exel для работы(.xlsx)?
        </TextBlock>
        <DataGrid Grid.Column="1" Grid.ColumnSpan="2" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False" 
                  CanUserSortColumns="True" 
                  CanUserResizeColumns="True" 
                  CanUserReorderColumns="False" 
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding Rows,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedRow}"
                  Visibility="{Binding IsExistFile,Converter={StaticResource dataGridVis}}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Идентификатор" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Факультет" Binding="{Binding Facultie.Name}" Width="auto"/>
                <DataGridTextColumn Header="Кафедра/Институт" Binding="{Binding DepartmentOrInstitute.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Корпус" Binding="{Binding Frame.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Номер аудитории" Binding="{Binding AudienceNumber.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Название аудитории" Binding="{Binding AudienceName.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Ответственный" Binding="{Binding ResponsiblePerson.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Инвентарный номер" Binding="{Binding InventoryNumber.Name}" Width="Auto"/>
                <DataGridTextColumn Header="ОС" Binding="{Binding OperatingSystem.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Материнская плата" Binding="{Binding Motherboard.Name}" Width="Auto"/>
                <DataGridTextColumn Header="ЦП" Binding="{Binding Cpu.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Видеокарта" Binding="{Binding VideoCard.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Диски" Binding="{Binding Disk.Name}" Width="Auto"/>
                <DataGridTextColumn Header="ОЗУ, ГБ" Binding="{Binding Ram.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Тип ОЗУ" Binding="{Binding RamType.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Список установленных приложений" Binding="{Binding ApplicationList.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status.Name}" Width="Auto"/>
                <DataGridTextColumn Header="Дата последнего обслуживания" Binding="{Binding LastServiceDate.Name}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
