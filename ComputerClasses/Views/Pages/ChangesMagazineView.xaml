<UserControl x:Class="ComputerClasses.Views.Pages.ChangesMagazineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerClasses.Views.Pages"
             xmlns:vm="clr-namespace:ComputerClasses.ViewModels.Pages"
             xmlns:converter="clr-namespace:ComputerClasses.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:ChangesMagazineViewModel}">
    <UserControl.Resources>
        <converter:EmptyMessageVisabilityConverter x:Key="MsgVis"/>
        <converter:ListVisibilityConverter x:Key="ListVis"/>
    </UserControl.Resources>
    <Grid>
        <TextBlock Visibility="{Binding Logs.Count,Converter={StaticResource MsgVis}}" Text="Пока ничего нет"/>
        <ScrollViewer Visibility="{Binding Logs.Count,Converter={StaticResource ListVis}}">
            <ItemsControl ItemsSource="{Binding Logs}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
